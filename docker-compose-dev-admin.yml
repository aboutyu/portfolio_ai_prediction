version: "3.9"

services:
  admin:
    build:
      context: ./admin
      args:
        - TARGET_ENV=development
    environment:
      - NODE_ENV=development
    container_name: admin-ai-dev
    ports:
      - "8080:8080"
    env_file:
      - ./admin/.env/development.env
    restart: always